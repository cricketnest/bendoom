--- a/bend2/comp.ts
+++ b/bend2/comp.ts
@@ -648,5 +648,5 @@
 function name_local(fl: File, k: Bend.Name): string {
-  const base = name_clean(k);
+  const base = "bend_" + name_clean(k).replace(/^bend_/, "");
   const n = fl.fresh.get(base) ?? 0;
   fl.fresh.set(base, n + 1);
   return base + "_" + n;
